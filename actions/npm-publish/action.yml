name: "Publish to npm"
description: "Publish package to npm with provenance"

inputs:
  npm-token:
    description: "npm access token for publishing"
    required: true
  registry-url:
    description: "npm registry URL"
    required: false
    default: "https://registry.npmjs.org/"
  dry-run:
    description: "Run npm publish with --dry-run (no package is actually published)"
    required: false
    default: "false"

runs:
  using: "composite"
  steps:
    - name: "Setup Node.js with registry"
      uses: actions/setup-node@v6
      with:
        node-version-file: .nvmrc
        registry-url: ${{ inputs.registry-url }}

    - name: "Pull latest changes"
      run: git pull
      shell: bash

    - name: "Install dependencies"
      run: npm ci
      shell: bash

    - name: "Publish to npm"
      run: npm publish --provenance ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
      env:
        NODE_AUTH_TOKEN: ${{ inputs.npm-token }}
      shell: bash
